# Drowziness-Detector
This script is a real-time drowsiness detection system that uses MediaPipe Face Mesh and OpenCV to monitor the Eye Aspect Ratio (EAR). It triggers an audio-visual alarm when eye closure duration exceeds a predefined threshold, effectively identifying signs of fatigue.
